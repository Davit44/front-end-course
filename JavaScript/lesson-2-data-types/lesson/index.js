/**
 * JS
 * (տվյալների տիպերը)
 */

// Dinamic(թույլ կամ դինամիկ տիպավորված լեզու) & Static(հաստատուն)

// Primitive types
// 1 number (... bolor tvery, NaN, Infinity, -Infinity)
// 2 string
// 3 boolean (true, false)
// 4 undefined
// 5 null
// +-

// "use strict"

var a = 15;
var b = 10;

var a, b, c, d;

f = 40;

document.write("Hello world");

// a = b = c = d

/**
 * Operators
 */

// 1. unar (+, 1)
// 2. binar (+, -, *, /) (=, +=, -=, *=, /=, ... (նշան=)), (>,<, >=, <=, ==, !=, ===, !==), (||, &&)
// 3. ternar

// && 
// (true && true) = true
// (true && false) = false
// (false && true) = false
// (false && false) = false


// || 
// (true || true) = true
// (true || false) = true
// (false || true) = true
// (false || false) = false

